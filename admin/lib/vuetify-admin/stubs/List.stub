<template>
  <v-card>
    <va-list
      :fields="{{ fields }}"
      :filters="{{ filters }}"
      :include="{{ include }}"
      flat
      v-slot="props"
      v-model="selected"
      :options.sync="options"
    >
      <va-datagrid
        :fields="{{ datagrid_fields }}"
        v-bind="props"
        v-model="selected"
        :options.sync="options"
      >
      </va-datagrid>
    </va-list>
  </v-card>
</template>

<script>
export default {
  data() {
    return {
      options: {},
      selected: [],
    };
  },
};
</script>
